# Required — get your key at https://platform.openai.com/api-keys
# Copy this file to .env.local and fill in your key.
OPENAI_API_KEY=sk-...

# The OpenAI SDK reads OPENAI_API_KEY automatically from the environment.
# Never prefix this variable with NEXT_PUBLIC_ — it must remain server-side only.
